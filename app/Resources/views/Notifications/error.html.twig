{% set messages = app.session.flashbag.get('error') %}

{% if messages is defined and messages %}
    <div class="alert alert-danger text-center">
        {% for message in messages %}
            <p>{{ message }}</p>
        {% endfor %}
    </div>
{% endif %}